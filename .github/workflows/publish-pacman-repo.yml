name: Publish pacman repo (GitHub Pages) [disabled]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This repo currently serves GitHub Pages via branch/Jekyll, so the Actions-based Pages deploy is disabled."
          echo "Pacman repo is available under /repo/vistula/os/x86_64/."
